# 트위터 UI 육성 게임

트위터 UI 기반 턴제 육성 게임. 하루=1턴, **팔로워 100만** 도달 시 스탯·이벤트 이력으로 멀티 엔딩 판정.
주인공은 **트위터 프리미엄 결제자**다 — 일주일 치 트윗 조회수로 정산을 받아 그 돈으로 생활한다.
설계 문서는 `docs/superpowers/specs/2026-08-17-twitter-game-design.md`(초기 설계 — 아래 "핵심 루프"가 최신).

## 핵심 루프 (불변)

**행동 하나 = 하루.** 컴포즈 박스 → 행동 선택 팝업 → **"이걸 트윗할까?"** → 다음 날.
- 별도의 "다음 날" 버튼은 없다. 행동을 마치면 하루가 자동으로 넘어간다.
- **날짜 전환에는 팝업이 없다.** 하루하루는 조용히 넘어가고, 딤팝업은 **주급날(정산일)에만** 뜬다 — 매일 뜨면 7번에 6번은 방해다. 날짜는 상단바에서 확인한다.
- `effects`(행동 효과)는 트윗 여부와 무관하게 항상 적용된다. `tweet.effects`는 트윗을 택했을 때만.
- **리스크는 전부 `tweet.effects`에 둔다**(논란성 상승·멘탈 소모). 트윗을 안 하면 안전하지만 팔로워도 안 는다 — 이 교환이 게임의 핵심 선택이다.
- 이벤트 대응(`event:…`)은 그 자체가 반응이라 트윗 여부를 묻지 않는다.

## 트윗 반응 (불변)

- **노출 → 좋아요 → 리트윗** 순서로 `engine.js`의 `engagement()`가 계산한다. **난수를 쓰지 않는다** — 노출은 팔로워×감각, 좋아요는 노출×(글빨 기반 반응률, 체감 감소), 리트윗은 좋아요×(유머 기반 비율). 같은 조건이면 같은 결과가 나와야 하고 회귀 테스트가 이를 고정한다.
- **조회수는 돈이 된다**(주간 정산의 유일한 재료 — 아래 "프리미엄 경제" 참고). 그래서 `engagement()`의 노출 수식을 만지면 경제 전체가 흔들린다. 반면 좋아요·리트윗은 여전히 표시 전용이고, **팔로워로는 아무것도 되먹임되지 않는다** — 팔로워 증감은 계속 `data/actions.js`의 수식만 정한다.
- 좋아요·리트윗·팔로우는 **알림에만** 뜬다(실제 트위터와 동일). `feed`의 `kind: "like"`/`"retweet"`/`"follow"` 항목이며, `ui.js`의 `TIMELINE_KINDS`/`NOTIF_KINDS`가 어디에 보일지 가른다 — 새 kind를 추가하면 두 목록과 `REACTIONS`(아이콘·문구)를 같이 손볼 것.
- **팔로우 알림은 `statChanges["팔로워"] > 0`에서 한 번에 만든다** — 트윗·홍보·이벤트 어느 경로로 늘든 이 한 곳을 지난다. 경로별로 따로 만들지 말 것. 인용할 트윗이 없어 `text: ""`이고, UI는 이때 미리보기 줄을 아예 안 그린다.
- 알림 문구는 X 형식: 좋아요가 3개 이상이면 이름 1개 + "외 N명", 2개면 이름 2개. `actors.length + others`가 항상 실제 수치와 같아야 한다.
- **답글(멘션)도 홈 타임라인에 안 뜬다.** 알림에서 보거나, 트윗을 눌러 상세(`#view-tweet`)로 들어가야 그 트윗에 달린 답글이 줄줄이 보인다.
  - 답글 수는 좋아요의 12%(NPC 수가 상한). 작은 계정은 0개가 정상이다 — 팔로워가 늘어야 스레드가 붙는다.
  - 답글은 `replyTo`로 원본 트윗의 `id`를 가리킨다. 트윗 `id`는 `state.tweetSeq`가 발급하며, **id가 없으면 상세를 열 수 없다** — `create()`가 옛 세이브의 트윗에 id를 채워준다.
  - 한 스레드 안에서 같은 계정이 두 번 답글하지 않는다(`pickActors`가 카테고리 일치 NPC를 먼저, 부족하면 나머지로 채운다. 중복 없음).

## 프리미엄 경제 (불변)

주인공은 프리미엄 결제자다. **조회수 → 정산금 → 생활비**가 이 게임의 두 번째 축이고,
노브는 전부 `data/economy.js`에 있다(주기 7일 / 1000회당 150원 / 프리미엄 1만원 / 생활비 1만원).

- **생활비는 하루 1만원, 무조건 나간다.** 멘탈 붕괴로 이틀이 지나면 이틀 치를 낸다.
  단 **`statChanges`에는 넣지 않는다** — `statChanges`는 "이 행동이 바꾼 것"이고 `previewAction`과 1:1로 맞아야 한다(회귀 테스트 있음). 고정 지출을 여기 끼우면 미리보기가 어긋난다.
- **정산은 7일마다 한 번.** `state.lastSettleDay`(아직 정산 안 한 주의 첫날)와의 차이로 재므로 멘탈 붕괴로 날짜가 건너뛰어도 한 주를 빠뜨리지 않는다. `day % 7`로 바꾸지 말 것 — 붕괴로 7일차를 건너뛰면 그 주가 사라진다.
- 정산 = `그 주 트윗 조회수 합 / 1000 × 단가 - 프리미엄 요금`. **트윗을 안 올린 주는 조회수 0이라 요금만 빠진다** — 그게 구독의 대가다.
- 정산 결과는 `advanceTurn`의 `settlement`(없으면 `null`)로 나오고, `kind: "settlement"` 피드 항목으로도 남는다. 엔진은 **일차와 정수 원만** 담는다 — "3월 2일"과 "1,000원" 표기는 `ui.js`의 `settlementEl()` 몫이다.
- **돈만 마이너스를 허용한다**(`clampStat()`). 파산은 게임 오버가 아니다 — 빚을 지고도 회생할 수 있어야 하고, 그 마이너스가 `loan_offer`(돈<0)·`debt_deep`(돈<=-50만) 이벤트를 부르는 트리거다. 나머지 스탯은 0이 바닥. `clampStat`은 `applyEffects`와 `previewAction` **두 곳에서 똑같이** 써야 미리보기가 안 어긋난다.
- **팔로워 수식의 `팔로워*n`이 복리 성장분이다.** 목표가 100만이라 스탯 선형 증가만으로는 500턴이 걸린다(실제로 재봤다). 초반엔 팔로워가 작아 이 항이 거의 0이고, 1만을 넘긴 뒤부터 곡선이 선다. 비율은 행동의 세기 순서를 따른다(떡밥 6% > 자료정리 4% > 밈·트렌드 3% > 나머지 1~1.5%, 휴식 0%).
- **정산금이 주 수입이어야 한다.** 협찬을 `팔로워*40` 같은 급한 기울기로 되돌리면 100만에서 한 방에 4천만원이 들어와 정산이 무의미해진다(그래서 `팔로워*2`로 낮췄다). 홍보는 정액이라 계정이 커지면 스스로 퇴장한다 — 이것도 의도다.
- 밸런스 확인은 `node test/sim.js`: 네 전략 전부 100만에 도달해야 하고(현재 182~479일), 전부 초반에 빚을 졌다가 회생한다(`최저` 열). 자기부양 전환점은 14주차 근처다.

## NPC 계정 (불변)

계정 17개가 `data/npcs.js`에 있다. **계정 하나 = 컨셉 하나**이고, 거기서 나오는 트윗만 올린다.

- **프로필 사진 경로는 핸들에서 나온다** — `@meme_bot99` → `assets/avatars/meme_bot99.svg`, 플레이어는 `me.svg`. 데이터에 경로를 적지 않고 `ui.js`의 `pfp()`가 만든다. 계정을 추가하면 **같은 이름의 svg를 같이 넣을 것** — 안 넣으면 그 계정만 아바타가 깨진다.
- 아바타는 DiceBear open-peeps(원본 Open Peeps, CC0)로 받아 **저장소에 넣어** 쓴다. 런타임에 외부 URL을 부르지 않는다 — 오프라인에서 깨지고 서비스가 사라지면 되돌릴 수 없다. 받는 방법·배경색 표는 `assets/avatars/README.md`.
- **계정(나·NPC)은 사진, 이벤트·시스템은 아이콘.** `avatarInner()`가 가른다 — 이벤트·시스템은 계정이 아니라서 사진이 없다. 답글·좋아요 알림의 아바타도 그 계정의 실제 사진이다.
- **아바타를 누르면 그 계정 프로필로 간다.** 클릭 위임이 `[data-account]`(핸들)를 읽는다 — 아바타는 트윗 안에 있으므로 `main.js`의 핸들러에서 **트윗 상세보다 먼저** 검사해야 한다. 안 그러면 아바타를 눌러도 트윗 상세가 열린다.
- **프로필 뷰(`#view-profile`) 하나로 내 계정과 남의 계정을 다 그린다.** `profileHandle`이 `null`이면 나, 핸들이면 그 NPC. 남의 프로필일 때만 뒤로가기 헤더가 뜨고, `활동 일차`는 숨고, 사이드바 `프로필`도 활성이 아니다. 사이드바 `프로필`은 `openProfile(null)`이라 **항상 내 프로필로 초기화된다**.
- 남의 프로필은 **내가 본 것만** 보여준다 — 게시물은 타임라인에 흐른 그 계정 트윗(`kind: "npc"`), 답글은 내 트윗에 달아준 것(`kind: "reply"`)이다. 둘 다 `state.feed`에서 `author`로 걸러 나오는 실제 데이터다. `bio`·`followers`가 없으면 프로필이 빈칸으로 뜨므로 계정을 추가하면 같이 넣을 것(회귀 테스트 있음).
- `followers`는 **표시 전용**이다 — 게임 수치에 영향이 없다.

### 트윗 (`tweets`) — 전 계정 공통

**트윗은 통째로 하나씩 저장한다.** 문장을 조합하지 않는다 — 조합하면 같은 문장이
길이만 다른 트윗으로 계속 재등장해서 타임라인이 지루해진다(실제로 그랬다).
규칙은 `data/npcs.js`의 `GAME_DATA.timeline`.

- **트윗 항목은 문자열이거나 `{ t: 본문, a: 속성 }` 객체다.** 객체는 그 트윗만 계정 기본 속성과 다르게 잡을 때 쓰고, 거의 안 쓴다. 중복 판정의 `src`는 **본문 문자열**이라 표기와 무관하게 같은 트윗은 한 번만 나온다.
- **보관함(`state.npcTweets[handle]`)이 그 계정의 타임라인이다.** 발견 시 `seed`개(과거) → 하루 `perDay`개 → 최대 `max`개(넘으면 오래된 것부터 밀려난다). 프로필은 `feed`가 아니라 이 보관함을 그린다.
- **`max`는 계정의 트윗 수보다 작아야 한다.** 보관함에 이미 있는 트윗은 후보에서 빠지므로(한 프로필에 같은 트윗이 두 번 뜨지 않게), 여유분이 없으면 그 계정은 새 트윗을 못 올린다. 현재 계정마다 28개 / `max` 24 — 여유 4개에서 매일 2개를 갈아넣는다. 프로필을 두껍게 하려면 트윗을 더 쓰고 `max`를 같이 올릴 것(회귀 테스트가 이 관계를 검사한다).
- 중복 판정은 **`src`(치환 전 원문)** 로 한다. `text`는 `{떡밥}`이 치환돼 원문과 달라서 `text`로 비교하면 같은 트윗이 두 번 뽑힌다.
- **타임라인 밀도와 보관함 성장은 별개다.** 발견한 계정은 전부 매일 보관함을 채우지만, 내 홈 타임라인에는 그중 `npcTweetsPerDay`개 계정의 **오늘 트윗 하나씩만** 흐른다. 못 본 건 프로필에서 본다(실제 트위터도 팔로잉 전원을 다 보여주지 않는다).
- **자라기 시작하는 기준은 `state.npcSeen[handle]`(그 계정을 처음 본 날)이다.** 만나기 전엔 보관함이 아예 없다 — 답글·좋아요·팔로우로 먼저 등장해서 발견되고, 그 다음 턴부터 매일 올린다. 발견 기록은 `advanceTurn` 끝의 한 곳에서 `feedItems`를 훑어 남기므로 등장 경로별로 따로 만들지 말 것.
- **새 게임은 `startFollowing: 0` — 아무도 팔로우하지 않은 채 시작한다.** 첫 화면은 빈 타임라인이고("타임라인이 조용합니다. 첫 트윗을 써보세요."), 트윗을 올려 반응이 오면 그때 계정을 만나 레일 `알 수도 있는 계정`에서 직접 팔로우해 나간다. **한 턴이면 계정을 만난다**(회귀 테스트가 이걸 고정한다) — 안 그러면 영원히 빈 화면이라 게임이 아니다.
  - **미리 깔아주는 경로는 코드에 남아 있고 따로 테스트한다.** `startFollowing`을 올리면 `create()`가 새 게임에서만 그 계정들을 1일차 발견 **과** 팔로우로 표시하고 보관함을 채운 뒤, 계정당 `openingTweets`개의 최근 트윗을 `state.feed`에 최신순으로 깔아준다. 노브를 되돌릴 수 있어야 하므로 이 경로가 죽지 않게 `loadWithFollowing(n)`으로 검사한다.
  - **세이브를 이어받을 때는 하지 않는다** — 옛 타임라인 위에 덧칠하게 된다(회귀 테스트 있음).
  - 미리 깔리는 트윗은 날짜가 1일차보다 이르고(내가 가입하기 전 글이니까) `kind: "npc"`라 알림에 안 잡힌다 — 시작부터 알림 뱃지가 뜨면 안 된다.
  - `main.js`가 시작 직후 저장한다. 안 하면 첫 턴을 돌리기 전에 새로고침할 때마다 뽑기가 다시 돌아간다.
- **발견 전 과거 트윗은 `feed`에 넣지 않는다.** 넣으면 홈 타임라인이 한 번에 20개로 도배된다. 날짜는 발견일 직전이라 게임 시작(3월 2일)보다 이른 2월로 갈 수 있다 — 내 계정보다 먼저 존재했다는 뜻이라 의도된 것이다.
- **트윗은 10~140자.** 줄바꿈은 데이터에 직접 `\n`으로 적는다(실제 트윗도 엔터를 눌러 쓴다). 렌더는 `.tweet .text`의 `white-space: pre-wrap`이 담당한다 — 지우면 줄바꿈이 공백으로 뭉개진다.
  - `{떡밥}` 치환은 길이를 늘린다(자리표시자 4자 → "번역기 오역 사건" 9자). 그래서 데이터만 재는 `check-assets.js` 외에 **실제 생성물의 길이를 재는 회귀 테스트**가 따로 있다.
  - 현재 길이 분포는 177 / 180 / 88 / 3(10~42 / 43~75 / 76~107 / 108~140자)로 긴 쪽이 얇다. 네 구간이 비지 않는 것만 검사한다 — 완전히 균등할 필요는 없고 실제 트윗도 짧은 쪽이 많다.
- **계정 하나 = 말투 하나.** 어미·문장부호·리듬이 계정마다 달라야 한다 — 전부 합쇼체로 쓰면
  계정 열일곱 개가 한 사람이 쓴 글처럼 읽힌다(실제로 그랬다). 현재 배치: ㅋ 섞인 인터넷 반말(짤줍),
  개조식 명사 종결(북마크중독), 공격적 반말(할말은함), 말줄임 해요체(새벽세시), 다정한 해요체+ㅎㅎ(맞팔해요),
  건조한 합쇼체+수치(엑셀장인), 극단적 단문(눈팅만함), 냉소적 중계(떡밥보관함),
  서정적 해요체(달밤), 리뷰 항목체(주말순례), 예보체(TL기상청), 자기검열체(노스포러),
  다정한 반말(뭉치와구름), 단호한 민원체(표기좀요), 문어체 습작(쓰다가지움), 무미건조 로그(별일없음),
  사료 기록체(옛기록보관 — "~라고 전한다" / "확인되지 않았다". 괴담을 무섭게 쓰지 않고 담담히 옮겨 적는다).
  **트윗을 추가할 때는 그 계정의 말투로 쓸 것** — 컨셉만 맞고 말투가 다르면 남의 계정 글처럼 읽힌다.
- **계정 간에 같은 트윗을 쓰지 말 것**(컨셉이 겹친다. 회귀 테스트가 잡는다). 계정마다 짧은 것(25자 이하)과 긴 것(90자 이상)이 하나씩은 있어야 한다.
- **남의 트윗도 `id`를 받아 상세로 열린다.** 보관함에만 있고 `feed`에는 없는 과거 트윗도 열려야 하므로 `renderTweetDetail()`은 `tweetLog`·`feed`·**보관함 전부**를 뒤진다.
- NPC 트윗(`kind: "npc"`)은 **타임라인에만** 뜬다 — 남의 트윗이라 알림이 아니다. 상세가 열리고, 좋아요·리트윗 버튼이 붙는다. 조회수는 없다(내 트윗만 정산 대상이라 그 수치를 안 만든다).
- `meme_bot99`는 배열 첫 항목이자 humor 카테고리의 첫 계정이다 — 회귀 테스트가 이 순서를 고정한다.
- 검증: `node test/check-assets.js` — 아바타 누락·안 쓰는 아바타·트윗 누락·길이 범위·계정 간 중복·max와의 관계를 잡는다. 계정을 건드렸으면 이걸 돌릴 것.

### 팔로우 (불변)

**발견과 팔로우는 다르다.** `npcSeen`은 "만났다"(프로필을 볼 수 있다), `following`은
"내 홈 타임라인에 흐른다". 예전엔 발견이 곧 팔로우였고, 그래서 만나는 족족 타임라인이 채워졌다.

- **보관함은 팔로우와 무관하게 자란다.** 발견한 계정은 전부 매일 `npcTweets`를 채우고,
  그중 **팔로우한** 계정만 `feed`에 흐른다. 실제 트위터도 내가 안 팔로우해도 그 계정은 계속 쓴다 —
  언팔로우해도 프로필에 들어가면 그동안의 트윗이 다 있다.
- **팔로우는 하루를 안 쓰고 스탯도 안 건드린다**(`toggleFollow(handle)`).
  바꾸는 건 "누가 내 타임라인에 흐르는가" 하나뿐이다. 팔로워를 주면 공짜 성장 경로가 된다.
- **새 게임은 팔로우 0에서 시작한다**(`startFollowing: 0`). 누구를 볼지는 플레이어가 고른다 —
  한 턴만 돌려도 계정을 만나므로 레일 `알 수도 있는 계정`에 팔로우할 거리가 바로 생긴다.
  노브를 올리면 발견 **과** 팔로우를 같이 켜야 한다. 둘 중 하나만 하면 첫 화면이 여전히 빈 타임라인이다.
- **옛 세이브는 발견한 계정을 전부 팔로우 중으로 채운다**(`create()`의 `missing.following`).
  안 채우면 세이브를 이어받은 순간 타임라인이 텅 빈다. 회귀 테스트 있음.
- UI 진입점 둘: 남의 프로필 우상단 버튼(`#profile-follow`)과 레일 `알 수도 있는 계정` 박스.
  둘 다 `[data-follow]`(핸들)를 달고 **`main.js`의 클릭 위임에서 `[data-account]`보다 먼저** 잡는다 —
  버튼이 계정 행 안에 있어서, 안 그러면 눌러도 프로필만 열린다(반응 버튼·아바타와 같은 함정).
- **내 프로필의 `팔로우 중` 숫자와 목록은 `followingList()` 한 곳에서 나온다.**
  `state.following`의 키를 그냥 세면, 지금 데이터에 없는 핸들이 세이브에 남았을 때
  "4인데 목록엔 3개"가 된다. 회귀 테스트 있음.
- **내 프로필의 `팔로우 중` 숫자를 누르면 목록(`#view-following`)이 열린다.** 레일과 같은
  `accountRow`를 쓰므로 행을 누르면 그 프로필로 가고, 뒤로가면 목록으로 돌아온다.
  **`팔로워` 숫자는 안 누른다** — 누가 날 팔로우하는지는 수치일 뿐 목록이 없다.
  같은 이유로 **남의 프로필엔 `팔로우 중`이 안 뜬다**(NPC가 누굴 팔로우하는지는 데이터가 없다).
- `팔로잉` 버튼은 hover에서 `언팔로우`로 갈린다 — `.f-on`/`.f-off` 두 span을 CSS가 갈아 끼운다.
  빨갛게만 변하고 글자가 그대로면 뭐가 일어날지 안 읽힌다.
- 팔로우 여부는 렌더 범위 변수 `followingNow`로 본다(`reactedNow`와 같은 이유 —
  `accountRow`까지 state를 흘리면 서명이 다 바뀐다).

### 남의 트윗에 반응 (좋아요·리트윗)

- **반응은 하루를 소모하지 않는다.** 날짜를 넘기는 건 `advanceTurn`뿐이고, 반응은 `toggleReaction(tweetId, kind)`이라는 별도 경로다. 좋아요 한 번에 하루가 가면 못 쓸 기능이 된다 — 이걸 `getActions()`나 컴포즈 흐름에 넣지 말 것.
- **팔로워엔 영향이 없다. 스탯은 트윗당 딱 한 번 센다.** 트윗마다 속성(`attr`)이 있고, 같은 속성 트윗 `reaction.perPoint`(5)개에 반응할 때마다 그 스탯이 +1 오른다. 팔로워를 주면 공짜 성장 경로가 되므로 팔로워는 여전히 안 준다.
  - 속성은 **계정의 첫 카테고리(`reactsTo[0]`)가 기본값**이고, `tweets`에 `{ t, a }` 객체로 적은 트윗만 예외로 덮어쓴다. 예외는 거의 안 쓴다.
  - 매핑은 `data/npcs.js`의 `reaction.attrStat`: humor→유머 / info→글빨 / daily→감각 / **bait→논란성**. 떡밥 계정을 챙겨보면 논란성이 쌓인다 — 반응이 순수한 이득이 아니라 **선택**이 된다.
  - **트윗 하나는 카운터를 한 번만 올린다**(`state.reacted[id].gained`). 좋아요든 리트윗이든 먼저 누른 쪽에서 오르고, **취소해도 되돌리지 않으며 다시 눌러도 추가되지 않는다** — 안 그러면 켜고 끄기로 무한 증식한다. `gained`는 **스탯을 실제로 센 경우에만** 찍는다(`attrStat`에 없는 속성이면 안 찍는다) — 안 그러면 데이터 오타 하나로 그 트윗이 영영 스탯을 못 주는 채로 세이브에 굳는다.
  - **스탯은 정수로 유지한다.** 소수(+0.2)를 누적하면 스탯 패널이 `글빨 7.0`처럼 전부 소수점을 단다. 그래서 `state.reactCount`(속성별 0~4)에 세고 5가 찰 때 +1한다.
  - **하루에 카운터가 오르는 반응은 `reaction.dailyCap`(8)개로 제한한다**(`state.reactDay = { day, used }`, 날짜가 바뀌면 자동 리셋). 트윗당 1회 상한만으로는 검색 화면(`allTweets`)이 모든 계정의 보관함을 한 화면에 모아주는 탓에 하루에 수백 개를 몰아 눌러 스탯을 폭증시킬 수 있었다(실측: 1일차 352회 반응에 논란성 +13 — topStat 엔딩 3종이 전부 막힘). **한도를 넘어도 좋아요·리트윗 자체는 정상 동작한다** — 카운터와 `gained`만 안 오른다(그래서 오늘 넘치게 누른 트윗도 내일 다시 누르면 셀 수 있다). 판정은 엔진에 두고 노브(`dailyCap`)만 `data/npcs.js`에 둔다.
  - **`data/endings.js`의 사이버렉카 임계값(`논란성>=300`)은 반응 그라인딩만으로는 못 넘고 어그로(의도된 사이버렉카 경로)는 넘는 값으로 잡았다.** 반응으로도 논란성이 쌓이게 되면서 예전 임계값 50은 bait 계정 프로필을 한 번만 훑어도 걸려 topStat 엔딩 3종을 사실상 막았다. `test/sim.js`의 `어그로` 전략(떡밥으로 논란성을 쌓는 의도된 경로, 논란성 740)과 `반응그라인딩` 전략(bait 트윗을 우선 노려 dailyCap 한도 안에서 좋아요만 누르는 경로, 논란성 233)을 실측해 그 사이로 정했다 — 임계값을 바꾸면 이 두 값을 다시 재고 사이에 있는지 확인할 것.
  - 진행은 **반응한 자리에 뜨는 게이지 토스트**가 보여준다(`UI.showGain`). 연타하면 새로 만들지 않고 하나를 옮기며 이어서 채운다. 이미 센 트윗은 `gain`이 `null`이라 아예 안 뜬다.
  - 트윗 우상단 **속성 배지**는 스탯 패널과 같은 아이콘·tone을 쓴다(`ATTR_STAT` + `STAT_STYLE`). 이미 센 트윗은 반투명(`.done`). `GAIN_MAX`·`ATTR_STAT`은 `data/npcs.js`의 값을 표기용으로 복제한 것이라 노브를 바꾸면 같이 맞춰야 한다.
  - **tone 색은 `.stat-row.tone-*` 6개 규칙의 셀렉터 목록에 `.attr-badge.tone-*`·`.gain-toast.tone-*`를 같이 추가해서 쓴다.** 안 하면 배지·토스트가 검정으로 렌더된다 — CSS에 새 tone 소비처를 추가할 때마다 걸리는 함정이다.
- 누른 기록은 `state.reacted = { "tw12": { like: true, rt: true } }`에 남고 세이브를 넘어 유지된다. 좋아요와 리트윗은 서로 독립이다.
- **반응 대상은 `kind: "npc"`이고 `id`가 있는 트윗만.** 답글은 자기 `id`가 없어서(`replyTo`만 있다) 대상이 아니고, 내 트윗은 읽기 전용 수치(`metricEl`)를 그대로 쓴다 — 카운트업 연출이 그 수치에 걸려 있다.
- 표시 수치는 **원본 + (내가 눌렀으면 1)**. 원본 값은 안 건드린다. 남의 트윗의 좋아요·리트윗 수는 `setCounts()`가 그 계정의 팔로워 규모에 비례해 **만들 때 한 번만** 뽑아 저장한다 — 렌더할 때마다 뽑으면 숫자가 매번 바뀐다.
- `create()`가 옛 세이브의 남의 트윗에 수치를 채워준다(보관함과 피드 **양쪽** — 저장/복원 뒤 서로 다른 객체다). 안 채우면 전부 0으로 보인다.
- **리트윗하면 내 타임라인과 내 프로필 게시물에 뜬다** — "내가 재게시했습니다" 라벨이 붙는다.
  - 피드에 항목을 새로 넣지 않는다. `ui.js`의 `myRetweets()`가 `state.reacted`에서 **파생**하므로 취소하면 저절로 사라진다 — 따로 지울 게 없다.
  - **원본이 이미 타임라인에 흘렀다면 그건 빼고** 리트윗 항목만 남긴다. 안 그러면 같은 트윗이 두 번 보인다(실제로 그랬다).
  - 줄 세우기는 `rtDay`(리트윗한 날)로 하고 타임스탬프는 **원본 트윗 날짜**를 그대로 쓴다. `toggleReaction`이 켤 때 `rtDay`를 적고 끌 때 지운다 — 안 지우면 다시 리트윗해도 옛 위치에 남는다.
  - 상세 화면에서는 라벨을 안 붙인다(원본 자체를 보는 화면이다).
- **리트윗 버튼은 바로 토글하지 않고 메뉴를 연다**(실제 X와 동일). 좋아요는 즉시 토글.
  - 메뉴는 `rtMenuFor`(열린 트윗 id) 하나로 관리하고 `reactRow()`가 그 트윗에만 그린다 — 트윗마다 숨은 메뉴를 두지 않는다.
  - 버튼을 다시 누르면 닫힌다. 그래서 이 경로에서 `closePopovers()`를 쓰면 안 된다 — 그게 리트윗 메뉴까지 닫아서 항상 열리기만 한다(실제로 겪음). 스탯·계정만 따로 닫을 것.
  - 스탯·계정 토글은 `stopPropagation`이라 위임에 안 걸린다 — 그 핸들러에서 `closeRtMenu()`를 직접 부르고, 닫혔으면 다시 그려야 사라진다.
  - 메뉴는 `.rt-wrap` 기준 **아래로**(`top: 100%`) 편다. 계정 메뉴처럼 위로 열면 피드 위쪽 트윗에서 화면 밖으로 나간다.
  - **인용은 넣지 않았다.** 인용은 내 트윗을 새로 쓰는 일이라 하루를 소모하는 별개의 행동이고 지금은 그 경로가 없다. 모양만 있는 항목은 넣지 말 것.
  - 같은 트윗이 여러 뷰(홈·상세·프로필)에 렌더되므로 **숨은 뷰에도 메뉴가 있다.** 테스트에서 `.rt-menu`를 잡을 땐 보이는 것만 고를 것.
- 클릭 위임 순서: **반응 버튼 → 아바타 → 트윗 상세.** 버튼이 트윗 안에 있으므로 먼저 잡지 않으면 눌러도 상세가 열린다(아바타와 같은 함정).
- `renderAll`이 매 렌더 시작에 `reactedNow`를 채운다. `tweetEl`까지 인자로 흘리면 `renderFeed`·`feedItemEl` 서명이 전부 바뀌므로 렌더 범위 변수로 둔다.

## 디엠 (불변)

**말을 튼 계정에만 열린다.** 대사는 `data/dms.js`의 `GAME_DATA.dm.accounts`에 있고
현재 5계정(`@mutuals_only` `@lurker_9` `@dawn_feels` `@cat_daily` `@rookie_writer`)뿐이다.
17계정 전부에 열면 대사가 트윗 476개만큼 필요하고, "이 사람과는 말을 튼 사이"라는 감각도 사라진다.

- **만난 계정(`npcSeen`)만.** 팔로우와는 무관하다 — 안 팔로우해도 DM은 온다(실제 X와 동일).
  안 그러면 첫날부터 대화방 5개가 놓여 있어 발견의 재미가 없다.
- **하루를 안 쓰고 스탯도 안 건드린다** — 팔로우와 같은 부류다. 트윗 476개에 좋아요를
  누를 수 있는 것과 같은 이유로, 대사 40개에 스탯을 주면 공짜 성장 경로가 된다.
- 대사 구조: `opens`(그 계정이 먼저 보내는 말) / `topics`(내가 고를 `say` + 답장 `back`) /
  `close`(고를 말이 떨어졌을 때). **한 번 쓴 건 다시 안 나온다**(`used`·`opened` 인덱스).
  인사말이 반복되면 봇처럼 읽힌다 — 회귀 테스트가 중복을 잡는다.
- **먼저 오는 DM은 턴당 최대 한 통**(`chance`). `feed`가 아니라 `state.dms`에 쌓이므로
  **알림에 안 뜬다** — 실제 X도 DM은 알림이 아니라 자기 뱃지를 쓴다.
- 상태: `state.dms = { "@h": { msgs: [{me,text,day}], used: [], opened: [], seen: n } }`.
  `seen`이 읽음 기준이고, 방을 열면 `markDmRead`가 맞춘다(알림의 `notifSeen`과 같은 규칙).
- 엔진 API 5개: `dmAccounts()` `getDmChoices(handle)` `sendDm(handle, idx)`
  `markDmRead(handle)` `unreadDms()`. **고를 말이 뭐가 남았는지는 엔진만 안다** —
  `ui.js`가 `used`를 다시 훑으면 규칙이 두 곳으로 갈라진다. 그래서 `renderAll(state, game)`으로
  엔진을 넘겨 렌더 범위 변수 `gameNow`에 둔다(`reactedNow`와 같은 방식).
- UI: `#view-dm` 하나로 목록과 대화방을 다 그린다(`dmHandle`이 `null`이면 목록 — 프로필과 같은 방식).
  진입점 셋: 사이드바 `쪽지`(항상 목록으로 초기화), 목록 행 `[data-dm]`, 프로필 우상단 쪽지 버튼.
- **선택지 바(`.dm-choices`)는 모바일에서 `bottom: 4.6rem`** — `0`이면 하단 탭바 뒤로 완전히
  들어가 안 보이고 못 누른다(실제로 겪음). `.main`의 `padding-bottom`과 같은 값이어야 한다.
- 말풍선 날짜는 **바뀔 때만** 찍는다. 매 줄에 붙이면 "3월 27일"만 열 번 읽힌다.

### 스토리 DM (불변)

`GAME_DATA.dm.stories`는 **순서가 있는 이야기**다. 기존 `accounts`(선택지 풀)와 다른 갈래이고
섞지 않는다 — 저쪽은 순서가 없고 이쪽은 순서가 전부라 같은 구조에 넣으면 규칙이 엉킨다.
현재 `@old_records`(괴담계) 하나뿐이다.

- **시작은 `requires`가 전부 맞는 턴에 확정으로** 온다(확률 없음). `reactions`는 그 계정 트윗에
  반응한 수(좋아요·리트윗 합산, 트윗당 1회 — `reactionsOn()`), 나머지는 스탯 비교식이다.
  현재 조건은 반응 5 + 감각 >= 12 + 글빨 < 20 — **"감각은 챙겼고 글빨은 안 챙긴"** 플레이어를 고른다.
  괴담계는 검증하려 드는 사람이 아니라 덥석 믿어주는 사람을 찾는다.
- 상태는 `state.dmStory = { "@h": { node, pending, done } }`. `node`가 지금 위치,
  `pending: { to, day }`가 예약된 지연 답장, `done`이 끝났는가다.
- **`choices`가 비면 대기 또는 끝이다.** 가르는 건 `end: true` — 없으면 대기(무언가 다른 것이
  진행시킨다), 있으면 종료다. 현재 `s2`가 대기이고 폐교 이벤트가 진행시킨다.
- **`delay: N`이면 답장이 N일 뒤 도착한다.** 기다리는 동안 방은 열려 있고 **아무 표시도 없다** —
  "기다리는 중" 같은 문구를 넣지 말 것. 침묵이 연출이라 시스템이 설명하면 죽는다.
- **스토리 진행 중인 계정은 확률 DM 후보에서 뺀다**(`inStory()`). 이야기 중간에 잡담이 끼면 안 된다.
- **스토리 선택도 하루를 안 쓰고 스탯도 안 건드린다**(기존 DM과 같은 규칙).
  이 이야기에서 하루를 쓰는 건 **산책하기 행동 한 번**뿐이다.
- `dmRoom()`은 `accounts`와 `stories` **양쪽**을 본다. 한쪽만 보면 스토리 계정의 방을 못 만든다.

### 행동에 걸린 이벤트 (불변)

기존 이벤트는 전부 확률로 알아서 발동하는데(`chance`), 스토리 이벤트는 **행동에 걸린 확정 이벤트**다.
`data/events.js`의 `trigger`에 두 키가 더 있다:

- **`action: "walk"`** — 그 행동을 한 턴에만
- **`dmStory: "@핸들:노드"`** — 그 스토리가 그 노드에 있을 때만(`atStoryNode()`)

선택지의 **`story: "@핸들:노드"`** 가 DM 스토리를 그 노드로 진행시킨다. 셋 다 범용 노브다 —
나중에 다른 계정 이야기도 다른 행동에 걸 수 있다.
**`checkCond`에서 `action`·`dmStory`를 건너뛰어야 한다** — 안 그러면 스탯 이름으로 오해한다.
- **이벤트 선택지의 `delay`에는 +1 보정이 필요하다.** 이벤트 선택은 `advanceTurn` 맨 앞에서
  처리되는데 그 시점 `state.day`는 아직 오늘이다. 턴이 끝나며 날짜가 넘어가고 곧바로
  `tickStories()`가 돌기 때문에, 보정이 없으면 `delay: 2`가 1일로 작동한다. `sendStory`는
  하루를 안 쓰는 경로라 보정이 필요 없다.
- **스토리 계정 판별을 "선택지가 비었는가"로 하면 안 된다.** 대기·지연 중에도 빈 배열이라,
  그걸로 가르면 기존 `getDmChoices`로 넘어가서 `data.dm.accounts[handle].topics`에서 터진다.
  하필 침묵 구간이 이야기의 핵심 연출이라 정확히 그 상태에서 화면이 죽었다. 그래서
  `isStoryAccount()`가 엔진에 있다.
- **`storyChoices`와 `getDmChoices`는 반환 형식이 같아야 한다**(`{ idx, label }`). 같은
  선택지 바에 그려지므로 다르면 UI가 분기해야 하고, 한쪽만 고치면 버튼이 빈칸으로 나온다.

## 아키텍처 규칙 (불변)

- **바닐라 JS, 빌드 도구 없음, 외부 의존성 없음.** 브라우저에서 index.html을 바로 연다.
- `assets/` — 저장소에 들어 있는 정적 파일(현재 아바타 svg 16개). **런타임에 외부 URL을 부르지 않는다.** 외부에서 받아온 소재는 받아서 커밋하고 출처·라이선스를 README에 남긴다.
- `js/engine.js` — 순수 게임 로직. **DOM을 절대 모름.** Node에서 직접 실행 가능해야 함.
- `js/ui.js` — 렌더링·클릭 처리만. 게임 규칙을 넣지 말 것.
- `js/main.js` — 초기화, 데이터 로드, localStorage 저장.
- `js/icons.js` — lucide 아이콘 SVG path 모음 + `Icons.svg(name)` / `Icons.mount()`.
- `data/*.js` — 모든 게임 콘텐츠(경제·행동·NPC·디엠·이벤트·엔딩). 순수 데이터 리터럴만, 로직 금지. **콘텐츠 추가/밸런스 조정은 코드가 아니라 여기서.** (json이 아닌 이유: file:// 에서 fetch가 CORS로 막힘)
  - 데이터 파일을 새로 만들면 `index.html`의 script 태그 **와** `test/engine.test.js`·`test/sim.js`의 `loadData()` 세 곳을 같이 고칠 것. 하나라도 빠지면 브라우저나 테스트 한쪽만 터진다.
- 엔진 공개 API 4개:
  - `getActions()` → `[{ id, label, kind, effects }]` (requires 미충족은 제외)
  - `previewAction(id)` → `{ label, effects, tweetEffects }` — 읽기 전용. **트윗 수식은 행동 효과가 적용된 뒤의 스탯으로 계산해야 실제와 일치한다**(안 그러면 팝업이 +20이라 하고 실제로 +26이 붙는다. 회귀 테스트 있음).
  - `advanceTurn(id, doTweet)` → `{ feedItems, statChanges, triggeredEvents, ending | null, settlement | null }`
  - `toggleFollow(handle)` — 팔로우 토글. **하루를 안 쓰고 스탯도 안 건드린다**(위 "팔로우" 참고). 모르는 핸들은 `null`.
  - `toggleReaction(tweetId, kind)` — 남의 트윗 좋아요/리트윗 토글. `kind`는 `"like"`/`"rt"`. **하루를 소모하지 않는다**(위 "남의 트윗에 반응" 참고). 반환값 `{ id, kind, on, gain }`의 `gain`은 `{ stat, count, leveled }`이거나 `null`(이미 센 트윗·모르는 속성). 모르는 입력은 `null`.
- **날짜는 엔진이 모른다.** 엔진은 `day`만 세고 표기는 전부 `ui.js`의 `START`/`dayToDate()`가 담당. 1일차 = 2026-03-02(월).
  - 표기 두 종류: **`shortDate()`("3월 10일")는 트윗·알림·정산 카드의 타임스탬프**, `dateLabel()`("3월 10일 (화)")는 날짜 자체가 주인공인 곳(상단바·주급날 팝업). 피드에 "N일차"를 쓰지 말 것 — 실제 X처럼 날짜로 적는다. 상단바만 턴 카운터라 "8일차 · 3월 9일 (월)"로 병기한다.

## UI 규칙 (불변)

- **이모지 금지.** 아이콘이 필요하면 lucide(https://unpkg.com/lucide-static@1.31.0/icons/<name>.svg)에서 받아 `js/icons.js`의 PATHS에 추가하고 `Icons.svg(name)`로 쓴다.
- **라이트 테마 단일.** 흰 배경이 기본이고 다크모드 대응은 하지 않는다. 색은 `css/style.css`의 `:root` 변수로만.
- **글씨 최소 12px (하한 불변).** 글씨 크기는 `:root`의 `--fs-xs`~`--fs-xxl`로만 지정한다. 전부 `max(--fs-min, …)`으로 감싸져 있어 스케일 노브를 낮춰도 12px 밑으로 안 내려간다. 규칙에 px/rem font-size를 직접 쓰면 이 하한이 뚫린다 — 새 크기가 필요하면 변수를 추가할 것.
- **스케일 노브는 `html { font-size }`** (`css/style.css` 첫 줄). 크기 조절 요청은 이 값 하나만 바꾼다. 아이콘·아바타·배너도 rem이라 같이 움직인다.
- 검증: `node test/check-css.js` — 하한값·변수 형태·raw font-size 유입, 그리고 **`:root`에 없는 변수**를 잡는다(정의 안 된 `var()`는 그 선언을 통째로 무효로 만들어 테두리가 조용히 사라진다 — 실제로 겪음). CSS를 만졌으면 이걸 돌릴 것.
- **아이콘 크기도 CSS가 정한다** (`.icon { width: 1.35em }` + 필요한 곳만 rem 오버라이드). `Icons.svg()`에 크기 인자는 없다.
- 작은 크기(13px 내외)에 쓸 아이콘은 path 2~3개짜리 단순한 것만 고른다(brain·dumbbell처럼 복잡한 건 뭉개짐).
- **트윗 줄 길이는 `.main { max-width: 38.5rem }`(≈500px) 하나가 정한다.** 폭을 바꿀 일이 있으면 여기만 고칠 것. `flex: 1`에만 맡기면 레일이 숨는 좁은 창에서 800px까지 늘어난다(그래서 max-width가 필요하다). 열 합은 사이드바 13.5 + 본문 38.5 + 레일 21 = **73rem**이고 `.app`의 max-width가 그 값이다 — 전부 rem이라 스케일 노브를 바꿔도 같이 움직인다.
  - `.app`에 `justify-content: center`가 있어야 레일이 숨어 남는 공간이 생겨도 열들이 가운데로 모인다.
  - 레일은 **`position: sticky` + `align-self: flex-start`**로 붙인다. `align-self`를 빼면 flex가 세로로 늘려서 sticky가 안 먹는다.
  - **960px 이하에서는 레일을 숨긴다**(`display: none`). 열 합(73rem ≈ 949px)보다 살짝 위가 기준이라 `.main`의 max-width를 바꾸면 이 값도 같이 봐야 한다. 모바일(≤640px) 규칙과 별개인 브레이크포인트고, **미디어 쿼리의 `rem`은 `html`의 font-size가 아니라 초기값 16px을 쓰므로** 여기서만 px으로 적는다.
  - 레일에는 **데이터가 있는 것만** 넣는다(모양 때문에 죽은 버튼을 추가하지 말 것).
  - **계정 목록은 이미 만난 계정만** 보여준다(`npcSeen`). 못 만난 계정을 띄우면 발견의 재미가 사라지고, 그 프로필엔 트윗이 하나도 없어서 눌러도 빈 화면이 뜬다. 행에 `data-account`를 달아 기존 클릭 위임이 프로필로 보낸다.
  - 그 목록을 팔로우 여부로 두 박스에 가른다: `팔로우 중인 계정`과 `알 수도 있는 계정`(`#rail-suggest-box`, 비면 박스째 숨긴다). 계정 행의 오른쪽 자리는 하나뿐이라 **팔로우 중이면 팔로워 수, 아니면 팔로우 버튼**을 둔다 — 좁은 레일(21rem)에 둘 다 넣으면 이름이 뭉개진다. 내 계정(@me)엔 버튼이 안 붙는다.
  - **트렌드는 실제 집계다.** `data.fills`의 소재가 **네트워크 전체**(모든 보관함 + 내 `tweetLog`)에서 몇 번 트윗됐는지 센다. 내 피드만 세면 첫날 표본이 10개뿐이라 대개 0건으로 비어 버린다. 보관함 항목이 피드에도 있으므로 **피드는 빼야** 중복 집계가 안 된다.
- **검색 입력창은 하나뿐이다** — 검색 화면(`#view-search`) 상단에 있다. 레일의 검색 상자는 입력창이 아니라 **그 화면으로 보내는 버튼**이다(입력창을 두 곳에 두면 값을 서로 맞춰줘야 한다). 레일 버튼에는 현재 검색어가 표시된다.
  - 사이드바에도 `검색` 항목을 둔다 — **모바일에선 레일이 숨으므로 이게 유일한 입구다.**
  - 검색 화면으로 갈 때 입력창에 포커스를 준다. 포커스는 **렌더가 끝난 뒤**에 줘야 한다 — 아직 `hidden`인 뷰의 입력창은 포커스를 못 받는다.
  - `oninput`으로 즉시 걸러낸다. `refresh()`가 입력창 값을 건드리지 않으므로 다시 렌더돼도 포커스와 커서가 유지된다.
- 검색 대상은 **트윗**(`allTweets()` — 내 `tweetLog` + 모든 보관함 + 피드의 답글, `id`로 중복 제거)과 **계정**(이름·핸들·소개글) 둘뿐이다. 미디어·리스트 탭은 데이터가 없어서 안 만든다.
  - 계정 탭은 **이미 만난 계정만** 보여준다(레일과 같은 이유 — 못 만난 계정 프로필엔 트윗이 없다). 내 계정도 검색된다.
  - 결과 행에는 `data-detail`(트윗)·`data-account`(계정)가 붙어 있어 **기존 클릭 위임이 그대로** 상세·프로필로 보낸다.
- 검색 탭은 `.search-tab[data-stab]`, 프로필 탭은 `.profile-tabs .tab-btn[data-tab]`이다. 둘 다 `.tab-btn` 모양을 쓰지만 핸들러가 다르므로 **선택자를 섞지 말 것** — `.tab-btn` 전체를 잡으면 검색 탭이 프로필 탭 핸들러로 간다.
- 스탯 패널은 사이드바 하단(계정 블록 위)에 상주. 프로필 화면은 실제 X 구조(배너→겹친 아바타→소개→탭)만 담는다.
- 사이드바 맨 아래는 **계정 블록**(아바타+나+@me+⋯). 누르면 로그아웃 팝오버가 뜨고, 로그아웃 = 세이브 삭제 + 새 계정 시작이다. 되돌릴 수 없으므로 `confirm()`을 한 번 더 받는다.
- **모바일(≤640px)은 실제 X 앱 구조**: 사이드바 → 하단 고정 독바(홈/알림/프로필/스탯/계정), 스탯·계정은 독바 버튼을 누르면 뜨는 팝오버, 딤팝업은 하단 시트로. 사이드바를 좁히는 방식으로 돌아가지 말 것.
- 스탯 팝오버와 계정 팝오버는 **배타적**이다 — 하나를 열면 다른 하나를 닫는다(`main.js`의 토글 핸들러). 팝오버를 추가하면 이 규칙에 같이 넣을 것.
- 팝오버는 **감싸는 컨테이너 기준 `bottom: 100%`**로 띄운다(`.account-block`). 사이드바 기준 `bottom: <고정값>`은 폰트 스케일이 바뀌면 버튼과 겹친다(실제로 겪음). 메뉴 문구가 사이드바보다 넓으면 실제 X처럼 오른쪽으로 넘기고 `white-space: nowrap`으로 줄바꿈을 막는다.
- 딤팝업 4종은 `.overlay` + `.card`를 공유한다: `#compose-modal`(행동 선택) / `#tweet-modal`(트윗 여부) / `#settle-modal`(주급날) / `#ending-overlay`. 모바일에서 `.overlay .card { max-width: none }`으로 데스크톱 상한을 풀어야 폭을 채운다.
- `#settle-modal`은 **자동으로 닫히지 않는다**(읽어야 하는 정보다). 확인 버튼이나 딤을 눌러야 닫히고, 닫힌 뒤에 카운트업 연출·엔딩이 이어진다(`main.js`의 `resolveTurn` → `finish`). 주급날 팝업과 피드의 정산 카드는 `fillSettleRows()` 한 곳에서 내역을 만든다 — 두 군데서 따로 만들면 숫자가 어긋난다.
- `#tweet-modal`은 딤 클릭으로 닫히지 않는다(의도). 두 버튼 중 하나를 골라야 하며, 안 그러면 행동이 적용되지 않은 채 남는다.
- 스탯 마운트 지점이 둘(`[data-stats]` 두 곳)인 이유: 데스크톱은 사이드바 상주, 모바일은 팝오버인데 CSS로는 DOM 부모를 옮길 수 없다. ui.js가 두 곳에 같이 렌더하고 CSS가 하나만 보여준다.
- **알림 뱃지는 개수가 아니라 점**이다(빈 span에 CSS로 찍는 동그라미). 안 읽은 알림이 하나라도 있으면 아이콘 우측 상단에 뜨고, 알림 화면을 열면 사라진다. 숫자를 다시 넣지 말 것.
  - 위치는 `.nav-icon`(아이콘을 감싼 `position: relative` 래퍼) 기준이라 데스크톱·모바일 양쪽에서 아이콘을 따라간다. 모바일 전용 좌표 보정은 필요 없다.
  - 뱃지를 `[data-icon]` **안**에 넣으면 `Icons.mount()`가 `innerHTML`로 덮어써 사라진다 — 반드시 형제로 둘 것.
  - 읽음 기준은 `state.notifSeen`(확인한 알림 수)이고, 어느 `kind`가 알림인지는 ui.js만 안다(`NOTIF_KINDS`). 그래서 `unreadNotifs()`/`markNotifsRead()`가 ui.js에 있고, 저장은 부르는 쪽(main.js의 nav 핸들러)이 한다.
- **구버전 세이브 보정에서 "없었던 필드"는 기본값을 채우기 전에 기록할 것.** `saved`와 `state`는 같은 객체라, 채운 뒤에 `saved.x === undefined`로 물으면 이미 값이 들어가 있어 영원히 거짓이다(실제로 겪음 — `lastSettleDay`·`notifSeen` 보정이 둘 다 조용히 죽어 있었다). `create()`의 `missing` 맵을 쓸 것. 회귀 테스트 있음.
- `#stats-toggle`도 `.nav-btn`이지만 `data-view`가 없다. 뷰 관련 순회는 반드시 `.nav-btn[data-view]`로 한정할 것 — 안 하면 `switchView(undefined)`로 전체 뷰가 사라진다.
- 함정: sticky였던 요소를 모바일에서 `position: fixed; bottom: 0`으로 바꿀 때 `top: auto`를 반드시 같이 준다. 데스크톱의 `top: 0`이 남으면 위아래로 늘어나 화면 전체를 덮고 클릭을 먹는다(실제로 겪음).
- 프로필 탭은 실제 데이터가 있는 것만 만든다(현재 게시물·답글 2개). 빈 탭·죽은 버튼을 모양 때문에 추가하지 말 것.
- 프로필 소개글은 `textContent`로 넣으므로 `<br>`이 아니라 `\n` + CSS `white-space: pre-line`으로 줄바꿈한다.
- 트윗 상세로 가는 클릭은 **위임**으로 걸려 있다(`main.js`의 `.tweet[data-detail]`). 렌더될 때마다 핸들러를 다시 붙이지 말 것. `뒤로`는 온 뷰로 돌아가고(`detailReturnView`), 독바/사이드바로 이동하면 상세가 닫힌다.
- **게시 버튼(`#post-btn`)은 사이드바에 있고, 요소는 하나다.** 데스크톱은 가로폭 버튼, 모바일은 우하단 + 플로팅 버튼 — CSS가 가른다(`position: fixed`로 독바 흐름에서 빠져나오고 독바보다 z-index가 높아야 한다). 버튼을 두 개 두면 핸들러도 두 벌이 된다.
  - 예전에는 컴포즈 박스가 홈에만 있어서 다른 화면에서는 하루가 안 넘어갔다. 지금은 **어느 화면에서도 행동할 수 있다.**
- **홈 타임라인 탭**: `추천` = 타임라인 전부(내 트윗·남의 트윗·이벤트·정산·내 리트윗), `팔로우 중` = `kind: "npc"`만(남이 쓴 글만 읽는 필터). 탭 선택은 화면을 옮겼다 와도 유지된다.
  - 홈 탭은 `.home-tab[data-htab]`, 검색 탭은 `.search-tab[data-stab]`, 프로필 탭은 `.profile-tabs .tab-btn[data-tab]`이다. 셋 다 `.tab-btn` 모양을 쓰지만 핸들러가 다르므로 **선택자를 섞지 말 것**.

## 작업 효율 (토큰 절약)

- 게임 규칙·수치 관련 작업은 해당 `data/*.js` 파일만 읽으면 됨. js 전체를 읽지 말 것.
- UI 스타일 작업은 `css/style.css`만, 렌더링 버그는 `ui.js`만 보면 됨.
- 검증: `node test/engine.test.js` + `node test/sim.js` (자동 플레이 시뮬레이션) + `node test/check-css.js` (글씨 하한) + `node test/check-assets.js` (계정·아바타). 브라우저 열기 전 이걸로 확인.
- NPC·프로필 사진 작업은 `data/npcs.js`와 `assets/avatars/README.md`만 보면 됨.
- 스탯 6종: **스킬** 글빨·유머·감각 / **자원** 멘탈·돈·논란성.
  - `SKILL_STATS`(engine.js 2행)는 topStat 엔딩 판정용 — 자원 스탯을 여기 넣으면 엔딩이 틀어진다.
  - **멘탈은 컨디션 배수**다 — `engine.js`의 `morale()`이 `팔로워 증가`(감소·타 스탯 제외)와 `engagement()` 반응률에 곱한다. 50이 기준점(×1.0), 상한 ×1.2 / 하한 ×0.4. 배수는 `evalEffect()` 한 곳에만 있어야 미리보기와 실제가 안 어긋난다(회귀 테스트 있음).
  - 돈 경로: **주간 조회수 정산이 주 수입**이고, 알바하기·협찬 검토는 정산금이 생활비를 못 덮는 초·중반을 버티는 수단, 홍보 돌리기는 초반 부스터다.
  - **돈은 원 단위 정수**로 저장한다(시작 300,000 = 무행동 30일치). 엔진·데이터는 순수 정수만 다루고, `1,000원` 같은 표기는 ui.js의 `statValue()`가 유일하게 담당한다 — 데이터에 "원"을 문자열로 넣지 말 것.
  - **돈은 유일하게 음수가 되는 스탯**이다. 0으로 자르는 코드를 새로 넣지 말 것.
- **스탯을 새로 추가하면** `initialState()`에만 넣으면 된다 — `create()`가 구버전 세이브에 빠진 스탯을 기본값으로 채운다. 이 보정이 없으면 그 스탯을 쓰는 수식이 `evalFormula`에서 ReferenceError로 터진다(회귀 테스트 있음).

## 언어

코드 식별자는 영어, 게임 콘텐츠 텍스트와 사용자 대화는 한국어.
